# 关节限位事件

从装配限位和关节轨迹中确定首次到达或超限事件。

## 公开 API

| 符号 | 类型 | 用途 |
| --- | --- | --- |
| [`detect_limit_events`](detect_limit_events.md) | 函数 | 从实际关节轨迹中确定每个已建模限位侧首次 reached/exceeded 事件。 |

## 模块规则

- 事件检测消费实际关节轨迹和装配中已经 author 的限位。
- 缺少限位或缺少轨迹不会产生事件，也不能据此声称限位检查通过。
- tolerance 必须是有限非负值。
