# KinCheckAPI v0.6.5 更新

v0.6.5 在显式结构模型上增加线性模态、频响和瞬态计算。`solve_modes()` 保存
质量归一化振型和刚体模态证据；`solve_frequency_response()` 保留复数 FRF 与
声明的阻尼；`solve_transient_response()` 记录位移、速度和加速度时程。
`estimate_psd()` 与 `compute_rms()` 强制等间隔采样、非负 PSD 和明确单位。共振
与振动限值检查区分频率接近风险和响应幅值限值。
